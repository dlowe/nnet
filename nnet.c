#include <stdlib.h>
#include <stdio.h>

int thingy(void) {
    return 1;
}

void bigrammer(FILE *f, int *bigram_counts, int *n_bigrams) {
    int c;
    while ((c = getc(f))) {
    }
}

int main(int argc, char **argv) {
    exit(0);
}
